# remove() replace()

## remove()
```python
s.remove('1')
# s에서 1 문자열을 제거하겠다. (1회)
```

## replace()
```python
s.replace('1','',3')
# 문자열에서 1을 빈문자열로 총 3회 바꾸겠다.
```
* 주의 ) 따로 대입을 해주어야 적용됨. s = s.replace,,,